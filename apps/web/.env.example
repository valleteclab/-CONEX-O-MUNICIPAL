# URL base da API Nest (sem barra final). Obrigatório em produção (build do Next).
# Railway (exemplo): https://api-production-488f.up.railway.app
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Slug do tenant municipal (mesmo DEFAULT_TENANT_SLUG na API)
NEXT_PUBLIC_DEFAULT_TENANT_SLUG=luis-eduardo-magalhaes
