/**
 * E2E completo (Supertest + AppModule + Postgres) fica para pipeline com Docker.
 * Smoke manual: `npm run docker:up` → `npm run start:dev -w api` → GET /api/v1/health
 */
describe('e2e placeholder', () => {
  it('workspace ok', () => {
    expect(true).toBe(true);
  });
});
