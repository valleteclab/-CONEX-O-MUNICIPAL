{
  "name": "conexao-municipal",
  "version": "0.1.0",
  "private": true,
  "description": "ConexÃ£o Municipal â€” monorepo (web + api + shared). Ver SDD-ConexaoMunicipal.md",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "dev:api": "npm run start:dev -w api",
    "build": "npm run build -w api",
    "build:all": "npm run build -w api && npm run build -w web",
    "start": "npm run start:prod -w api",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@nestjs/cli": "^10.0.0",
    "@nestjs/common": "^10.4.22",
    "@nestjs/core": "^10.4.22",
    "@nestjs/platform-express": "^10.4.22"
  }
}
