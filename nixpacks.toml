# Railway / Nixpacks — monorepo Node 20 (workspace)
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build -w api"]

[start]
cmd = "npm run start"
